import 'package:flutter/material.dart';

// Renders: "A : B :: ? : ?" header + pair options below
class AnalogyCard extends StatelessWidget {
  const AnalogyCard({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
